# Leave empty to use Vite's dev proxy (see vite.config.js) to http://localhost:8080.
# Set this if the backend is deployed elsewhere, e.g. VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=https://streamflix-backend-qddk.onrender.com
